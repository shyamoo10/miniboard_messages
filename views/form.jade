extends layout

block content
 h1= title 
     form(action="/new", method="post")
      label(for="user") Name
      input(type="text", id="user", name="messageUser", required)

      label(for="message") Messages
      input(type="text", id="messages", name="messageContent", required)

      input(type="submit", value="Submit")